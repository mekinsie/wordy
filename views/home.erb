<h1>Welcome to Wordy!</h1>


<h2>You can create a word and give it your own definition.</h2>

<a href="/new">Add a new word</a>

<h3>Your Words:</h3>
<% if @word_list.any? %>
  <ul>
    <% @word_list.each do |word| %>
      <li><a href="/word/<%= word.id %>"><%= word.word %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no words to display.</p>
<% end %>
